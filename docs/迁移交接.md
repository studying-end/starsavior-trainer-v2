# 迁移交接 — 新会话接手指南

> **新会话第一句**：`继续 starsavior-trainer-v2 开发，看 Task Master 下一个任务`
> → `next_task` 指向 T18（pending，单测迁移 + 实跑完整 run 验证 —— 最终任务）。本文档让你无需重新摸索即可正确推进。

## 1. 精确进度（17/18，234 测试全过）

**done**：T1 通用框架 / T2 capture / T3 ocr / T4 vision / T5 executor / T6 screens.base / T7 注册表骨架 / T8 classifier+handler注册 / T9 文字工具 / T10 21画面parser / T11 config资产 / T12 policy骨架 / T13 policy各决策 / T14 SKILL_SELECT 决策接上 decide_skill（坑 #35）/ T15 4检视器 / T16 round_tracker+live_loop（拆 cli/{pause,blue_parsers,runtime,live_loop}.py 4 文件）/ T17 offline_harness + 3 小工具 CLI + web/app.py（拆 process_manager）+ 模板/静态资源

**in-progress**：无

**pending**：T18（单测迁移 + 实跑验证，next_task 指向 —— 最终任务）

## 2. 立即下一步：T18 单测迁移 + 实跑完整 run 验证（最终任务）

T16（live_loop）+ T14（SKILL_SELECT）+ T17（offline_harness + Flask web）**均已完成**（234 测试全过）。详见 [设计方案](设计方案.md) §7/§8/§9。

**T18**：迁移旧 148 测试 + 新增拆分子模块测试（18.1，可离线推进）；真实 OCR 准确率评估（坑 #37，18.2）+ SHOP/BATTLE/REGION_MOVE 坐标实跑核对（坑 #34，18.3）+ **一次确认成功的完整 run**（坑 #38 最大风险，18.4）+ 耐力耗尽跨画面导航验证（坑 #33）——后三者需真机 + GPU，非纯离线可完成。

---
## 2a. T16 已完成参考 — main 12 步迭代 + 拆分映射（T17/T18 离线回放用）

旧源：`d:/chengfeng/starsavior_trainer/cli/live_loop.py`（895 行）。**已拆 4 文件**（满足 ≤400 行）：

| 新文件 | 旧源行 | 内容 |
|---|---|---|
| `cli/pause.py` | 146-215 | `PauseController` + `install_pause_hotkey`（F11, trigger_on_release 防翻转，坑 #17；F9→F11 见 memory pause-hotkey-f11） |
| `cli/blue_parsers.py` | 525-866 | `_read_screen_payload_ocr` / `_read_screen_payload_blue` + 8 个 `_X_blue`（shop/training_hub/training_select/rest_submenu/commission_select/relic_choice/event_choice/dialogue）+ `_BLUE_PARSERS` 字典 + `_screen_to_prefix` |
| `cli/runtime.py` | 110-145 + 869-890 | 鼠标急停（`_is_corner_point`/`_mouse_at_screen_corner`，margin=120）+ helper（`_create_ocr`/`_find_or_exit`/`_print_windows`）—— 行数规范驱动的二次拆分 |
| `cli/live_loop.py` | 1-102 + 216-524 + 893-895 | 常量（`_ADVANCE_SCREENS`/`_ADVANCE_SLEEP=0.35`/`_TRAINING_SELECT_SLEEP=0.5`）+ `main`（12 步主循环） |

**迁移顺序**：pause.py（独立）→ blue_parsers.py（独立）→ live_loop.py（import 前两者 + 所有已迁模块）。

**main 12 步迭代**（架构文档 §4，严格复刻）：
1. 急停检查（鼠标 4 角 margin=120）
2. F9 暂停（trigger_on_release）
3. 截图 capture_window（PrintWindow，缩放 2560×1440）
4. scale_region_profile
5. classify_hybrid（实跑默认）
6. UNKNOWN 处理（连续 ≤4 帧点中心推进，>4 帧 pause）
7. parse payload（走 HANDLERS；角色列表走 bbox）
8. 回合追踪（TRAINING_HUB 读日期 + RANK 更新 character_rank）
9. 决策（检视器优先，返回 None 才 policy.decide）
10. CHARACTER_SELECT 重复点击 3 次 → 强制 pause（防死循环）
11. 执行前**再查急停** → activate_window → map_action_to_rect → execute
12. 智能睡眠（过场 0.35s 连点 / TRAINING_SELECT 0.5s / 其他默认 3s）

**坑规避**：C 类急停双保险（17-19，execute 前再查）、UNKNOWN ≤4/>4 帧、智能睡眠分级。

## 3. 已迁模块清单（import 关系）

```
starsavior_trainer/
├── 通用层: logging_setup models regions image_regions manifest fixtures capture ocr vision executor
├── 文字层: text_constants ocr_reader text_utils（含 _or_none/_ocr_int_token_to_int）
├── 分类层: classifier（核心 classify_*) classifier_anchors（_match_screen/常量） classifier_signatures（14 消歧）
├── screens/ : __init__（HANDLERS 21 + rebuild_anchor_handlers） base（DelegatingScreenHandler）
│              character_select blessing relic training rest event commission shop region_move battle simple
├── policy/ : engine（TrainerPolicy 多继承 7 Mixin + _is_iterable_of） config event_db
│             character_select training simple blessing event relic_commission shop_skill
├── inspectors/ : training_inspector commission_inspector shop_inspector blessing_inspector
├── round_tracker
├── cli/ : __init__  pause  blue_parsers  runtime  live_loop  offline_harness  capture_once  crop_regions  read_regions  ← start_journey 未迁（依赖废弃 journey_navigator）
└── web/ : __init__  app（路由+main）  process_manager（子进程/日志/配置）  templates/index.html  static/{script.js,style.css}
```

**关键 import 关系**（迁 live_loop 时按此调）：
- `from starsavior_trainer.screen_reader import X` → 按 X 归属改 `screens.<name>` 或 `text_utils` 或 `ocr_reader`
- `from starsavior_trainer.policy import _is_iterable_of` → `policy.engine`
- classifier 用 `classifier_anchors._match_screen`（lazy import `screens.ANCHOR_HANDLERS`，循环依赖，已处理）
- screens/__init__ import classifier_signatures + policy.engine + screens/*.parse + base

## 4. 迁移操作手册（每任务循环）

1. `mcp__taskmaster-ai__next_task` 取任务 → `set_task_status` in-progress
2. `Read d:/chengfeng/<旧源>` + `Read` 新占位（若存在，Write 前置要求）
3. `Write` 新文件（原样搬，**调整 import 路径**；中文 `\uXXXX` → 直接字符）
4. `Write tests/test_<name>.py`
5. `Bash`: `cd d:/starsavior-trainer-v2 && python -m unittest discover -s tests -t . -p "test_*.py" 2>&1 | tail -6`
6. `set_task_status` done（含 subtasks）
7. **非 1:1 改动 → 更新 [变更记录](变更记录.md) + [设计方案](设计方案.md)**（纯搬迁不记录）

## 5. 关键规则（CLAUDE.md + memory，不可违背）

- **架构契约**：OCR/CV 只产 Observation 绝不直接产 Action；screens 注册表是唯一调度枢纽。
- **代码规范**：单文件 ≤400 行（超了拆）、嵌套 ≤4 层、优先 Edit 不重写。
- **双文档规则**：非 1:1（新增/优化/修改逻辑·结构·文案）→ 变更记录（改了什么）+ 设计方案（方案理由）；纯 1:1 搬迁（含拆分必然的 import 调整）不记录。已记：T6 弃占位/T7 rebuild/中文转义/测试重组/T12-13 mixin/T8 classifier 拆分。
- **包名** `starsavior_trainer`（与旧一致，import 路径不变）。
- **MCP provider** claude-code；`expand_task`/`parse_prd` 报 PERPLEXITY_API_KEY 缺失 → 重连 MCP。

## 6. 剩余任务要点

- **T16 live_loop**：✅ 已完成（拆 4 文件，见 §2a + 设计方案 §7）。main 是核心（~310 行），blue_parsers 是 blue 模式 parser 集合。
- **T14 SKILL_SELECT 决策**：✅ 已完成（接上 decide_skill 学最优技能，坑 #35，见设计方案 §8）。
- **T17**：✅ 已完成（offline_harness + capture_once/crop_regions/read_regions + web/app.py 拆 process_manager + 模板/静态资源，见设计方案 §9）。start_journey 未迁（依赖废弃 journey_navigator，app.py 保留调用、运行时报错但 live_loop 接着跑）。
- **T18**：迁移旧 `tests/`（148 测试，按新模块调整 import）+ 真实 OCR 准确率评估（坑 #37）+ 坐标核对（坑 #34）+ **一次完整 run**（坑 #38 最大风险）。
  - **subtask 18.1 进度（2026-07-04）**：旧 11 测试文件均有新对应。**classifier 测试已补齐**（`tests/test_classifier.py` 26 测试，架构核心消歧回归，此前 0 覆盖）；5 个视觉测试因 4 张截图未入库延后到 subtask 2（详见变更记录同日条目）。其余 screen_reader/inspectors/policy 等主体已覆盖（新 234 测试 > 旧 148）。待续：逐一核对剩余旧用例是否全迁。

## 7. 测试

`python -m unittest discover -s tests -t . -p "test_*.py" -v`（stdlib，无需 pytest）。当前 234 测试。NoopOcrEngine 让管线测试无需 GPU。

## 8. 旧源位置

`d:/chengfeng/starsavior_trainer/`（旧项目根）。所有待迁文件在此。旧 tests 在 `d:/chengfeng/tests/`。

## 9. ✅ 已完成：支援卡羁绊达标 → 人头不作数（§22.5，2026-07-04）

**需求**：现有"前 20 回合优先人头最多的训练"（[training_inspector.decide_early_game](file:///d:/starsavior-trainer-v2/starsavior_trainer/inspectors/training_inspector.py)）基础上加规则——**当某支援卡的羁绊进度条变黄（达标）后，其人头从「作数」变「不作数」**，从而优先选其他未达标支援卡所在的训练。

**目的**：让全员支援卡尽快羁绊达标 → 触发**闪光训练**（提供大量训练值）。原项目有闪光训练相关代码（待在 `d:/chengfeng/` 找）。

**截图**：`screenshots/支援卡羁绊达标.png`（保护训练有 1 人头 + 人头下方进度条已黄 = 达标）。可测试 count_heads 模板匹配能否匹配上该人头。

**实现线索**：
1. 羁绊达标检测：人头下方的进度条颜色（黄=达标，其他色=未达标）。CV 检测每张训练卡人头下方的进度条颜色。
2. count_heads（[vision.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/vision.py)）扩展：达标人头计为不作数（类似现有 non_counting_head_N.png 机制，但这次按"羁绊达标"动态判定，不是固定模板）。
3. decide_early_game：优先「作数人头（未达标）最多」的训练。
4. 先确认：黄色进度条 CV 阈值 + 闪光训练在旧项目的代码位置。

**命名红线**（§22.0）：endurance=耐力(训练资源)，stamina=体力(HP属性)，勿混。

## 10. ✅ 已完成：闪光训练检测（§22.6，2026-07-04）

**需求**：训练按钮变金黄（闪光训练，大量训练值）时识别；讨伐委托优先闪光（用户硬规则）。

**实现**：`vision.detect_flash_training`（training 按钮 R-B>25 金黄像素>25%）+ `TrainingHubStatus.has_flash_training` + `parse_training_hub` 检测/narrate + `_decide_training_hub` narrate。

**优先级**：讨伐委托 > 商店 > 休息 > 训练（narrate 闪光；值不值得由 SELECT §22.4 线性模型决定）。真机验证 flash_check → flash=True + commission_alert=True → 走讨伐（正确）。
