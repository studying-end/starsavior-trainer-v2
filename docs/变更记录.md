# 变更记录

> 规则：仅记录偏离「原样 1:1 迁移」的**新增 / 优化 / 修改**。纯搬迁（不改逻辑/结构/文案）不记录。
> 每条变更的方案与理由见配套 [设计方案.md](设计方案.md)。
> 起始：2026-06-27（迁移进行中）。

## 2026-06-27

### T6 — screens/base.py 用旧实现替换骨架占位草稿
- 骨架阶段写的 base.py 是「设计草稿」（Any 占位、@property 接口、无 DelegatingScreenHandler），Protocol 签名与旧实现不一致。
- 决策：以旧实现（经 148 测试验证）替换占位草稿。
- 详见 设计方案.md §1。

### T7 — screens 注册表新增 rebuild + 原地替换
- 新增 `rebuild_anchor_handlers()`（旧版无；旧版模块加载时一次 sorted）。
- 修改：rebuild 用 `ANCHOR_HANDLERS[:] = sorted(...)` 原地替换（旧版 `ANCHOR_HANDLERS = sorted(...)` reassign）。
- 详见 设计方案.md §2。

### 中文 \uXXXX 转义 → 直接中文字符
- 涉及：fixtures.py（demo 数据）、text_constants.py（ALIASES）。
- 详见 设计方案.md §3。

### 新增 / 重组单元测试
- 新增（旧项目无对应）：test_screens_base、test_screens_registry、test_text_utils、test_ocr_reader。
- test_regions 增 load_region_profile / load_manifest 用例。
- test_executor / test_vision 从旧混合测试按新模块拆分迁入。
- 详见 设计方案.md §4。

### T12/T13 — policy.py 拆分为子包 + Mixin 模式
- policy.py（828 行）拆为 policy/{config, event_db, engine}.py + 7 个决策 Mixin（character_select/training/simple/blessing/event/relic_commission/shop_skill）。
- decide_X 从 TrainerPolicy 单类方法 → 各 Mixin 方法；TrainerPolicy 多继承各 Mixin。
- 详见 设计方案.md §5。

### T8 — classifier 拆分 + handler 注册（policy 闭环）
- classifier.py（784 行）拆为 classifier.py 核心（classify_by_ocr/blue_button/hybrid）+ classifier_anchors.py（anchor 常量 + _match_screen + classify_by_filename）+ classifier_signatures.py（14 消歧 signature + 密度 helper）。
- screens/__init__.py 注册 21 DelegatingScreenHandler（_decide_X 薄包装整合 parse(T10)+decide(T13)+has_anchor(T8)），policy.decide 闭环分发。
- 详见 设计方案.md §6。

## 2026-06-28

### T16 — live_loop.py 拆 3 文件 + import 重映射
- 旧 cli/live_loop.py（895 行，超 400）拆为 cli/{pause, blue_parsers, runtime, live_loop}.py：
  - pause.py：PauseController + install_pause_hotkey（F9，trigger_on_release 防翻转，坑 #17）。
  - blue_parsers.py：_read_screen_payload_ocr/blue + 8 个 _X_blue + _BLUE_PARSERS（9 项）+ _screen_to_prefix。
  - runtime.py：鼠标急停（_is_corner_point/_mouse_at_screen_corner，margin=120）+ helper（_create_ocr/_find_or_exit/_print_windows）。
  - live_loop.py：常量（_ADVANCE_SCREENS/_ADVANCE_SLEEP=0.35/_TRAINING_SELECT_SLEEP=0.5）+ main 12 步。
- runtime.py 抽出原因：3 文件拆分后 live_loop.py 仍 467 行（超 400），按规范把无状态急停 + CLI helper 再抽到 runtime.py，live_loop 降至 398 行聚焦 main 编排。
- import 重映射（screen_reader 已拆，原模块不存在）：classify_*→classifier / classify_journey_origin_by_visual+journey_origin_visual_scores→classifier_signatures；parse_first_int→text_utils；parse_battle→screens.battle；parse_character_select_bbox→screens.character_select；RegionOcrReader→ocr_reader；HANDLERS→screens；TrainingInspector/ShopInspector/CommissionInspector→inspectors.*；TrainerPolicy+_is_iterable_of→policy.engine；_detect_red_text/_detect_yellow_text→vision.detect_red_text/detect_yellow_text（去 underscore 前缀）。
- 裁掉 main 未引用的死 import：旧 `from screen_reader import parse_*` 大块（21 个）中 main 仅用 parse_first_int，其余 payload 解析已走 HANDLERS 注册表；screen_reader 又已不存在，不裁则无法 import。
- _screen_to_prefix 旧项目即死代码（仅定义无调用），按迁移规范保留并加 NOTE 注释（既有死代码不删）。
- 详见 设计方案.md §7。

### T14 — SKILL_SELECT 决策接上 decide_skill（坑 #35）
- 旧项目（及 T13 迁移后）`_decide_skill_select` 硬编码点 `skill_select_close_button` 退出，使已迁的 `policy.decide_skill`/`skill_score` + `parse_skill_select` 一直闲置（死代码）。旧注释「技能学习留到跑马完成」把✕退出当作前期策略。
- 决策（用户确认「学最优技能」）：`_decide_skill_select` 改为 payload 为 SkillOption 列表时调 `policy.decide_skill`，命中可学技能（返 click）就学；全部已习得/无可学（decide_skill 返 pause）→ 转✕退出，避免卡死技能界面。
- 行为变化：进技能/潜质界面由「总是点✕退出」→「按 build profile 评分学最优技能，无则退出」。
- 详见 设计方案.md §8。

### T17 — web/app.py 拆 process_manager + 迁 4 CLI 工具（作用域按用户决策）
- 旧 web/app.py（482 行，超 400）拆为 web/app.py（387，路由+main）+ web/process_manager.py（119，子进程/日志/配置基础设施：current_process/process_lock/log_queue 单例 + _run_command + _list_region_profiles/_load_characters + BUILD_PROFILES/CLASSIFY_MODES 常量）。app.py 路由通过 process_manager 共享同一份模块级单例状态。
- 迁 4 CLI 工具（用户确认「迁 3 小工具」作用域，含 offline_harness 共 4 个）：cli/offline_harness.py（classify_by_filename→classifier_anchors，TrainerPolicy→policy.engine）+ cli/capture_once.py（1:1）+ cli/crop_regions.py（1:1）+ cli/read_regions.py（RegionOcrReader：screen_reader→ocr_reader）。
- web 静态资源原样复制：templates/index.html（281）+ static/script.js（601）+ static/style.css（404）。
- **start_journey 不迁**（依赖废弃 journey_navigator，架构文档 §3.5 标「废弃半成品，已被 live_loop 覆盖」）：app.py 训练/旅程 tab 保留 `[sys.executable, "-m", "...start_journey"]` 调用，运行时该子进程因模块不存在报错，但训练 tab 的 live_loop 步仍接着跑（journey 本是 no-op）。已知缺口。
- process_manager._run_command 流式读完 stdout 后显式 close（旧代码未关，FD 泄漏 ResourceWarning）。
- 详见 设计方案.md §9。

### OCR 引擎替换：PaddleOcrEngine → RapidOcrEngine（ONNX Runtime GPU）
- 实机验证（2560×1440 大厅帧）暴露：paddleocr 2.7.3 对游戏 UI 全乱码幻觉（全图 194 行随机汉字），paddleocr 3.x 又撞 paddlepaddle-gpu 3.0.0b2 (beta) 的 PIR 崩溃（`pd_op_to_kernel_pass: op kernel output args`）。两者皆不可用；旧 148 测试用 NoopOcrEngine（坑 #37），真 OCR 从未被验证。
- 换 RapidOCR（rapidocr-onnxruntime，PP-OCRv4 模型走 ONNX Runtime，GPU 经 onnxruntime-gpu/CUDAExecutionProvider）：同一大厅帧 "训练/委托/休息/RANK 12/3月上旬/力量 180/1250/…" 全对；分类由误判 post_training(1.00) → 正确 training_hub(0.83) + 决策「点训练按钮」。
- 改动：ocr.py `PaddleOcrEngine` → `RapidOcrEngine`（use_cuda=True，GPU 失败回退 CPU）；runtime `_create_ocr` / read_regions（`--engine rapid`）/ test_live_loop_helpers 跟改；live_loop `--use-paddle` flag 名保留（web UI 在用）但实际走 RapidOCR，mode_label paddle→rapid；process_manager CLASSIFY_MODES 标签 PaddlePaddle→RapidOCR。**坐标/锚点/迁移代码未动**（坐实根因是 OCR 引擎，非坐标——坑 #34 在 2560×1440 上洗清）。
- requirements：去 paddleocr/paddlepaddle-gpu，加 rapidocr-onnxruntime + onnxruntime-gpu；环境卸载 paddleocr/paddlex/paddlepaddle-gpu（code 无 import paddle）。
- 详见 设计方案.md §10。

## 2026-07-02

### 训练人头计数 count_heads（改模板匹配法）
- `vision.count_heads(image, search_region=None)`：数训练选择画面"作数"支援卡人头。支援卡头像=**固定角色**（每回合只分配到不同训练），存为 `config/assets/counting_head_*.png`；对每个模板 matchTemplate（默认**全图**，避免限区把头切边漏匹配），>0.85 即在场，数命中数。**不作数**头像(non_counting_head_*.png)不在计数集，自动排除。
- 实机验证两套真值全对：(1,1,1,0,0) 与 (0,0,2,1,0)（含同卡多头 guts=2 + 不作数头 speed 自动排除）。命中分 0.96-1.0 vs 非命中 <0.78，干净分离。
- 演进：std/edge 法（0/1，多头/不作数失效）→ 模板匹配（精确多头 + 排除不作数 + 不怕位置变）。配 config/assets/ 模板库（counting_head_1/2/3 = guts×2+wisdom×1；non_counting_head_1/2）；新头像可自行裁追加。
- 全套测试过。详见 设计方案.md §11。

### 早期游戏训练决策 + 住处休息回心情
- `TrainingInspector` 加早期游戏模式（`current_round <= EARLY_GAME_HEAD_ROUNDS=20`）：逐张点 5 卡读 `count_heads`，`decide_early_training` 选人头最多（攒支援卡羁绊）；全<=1 则置 `_needs_rest` + `_rest_for_mood` 点返回回大厅休息。
- `decide_early_training(head_counts)`：max<=1 → None(休息)；否则选人头最多（平局 力量>体力>韧性>专注>保护）。实机验证当前帧 (0,0,2,1,0) → 选 guts ✓。
- `decide_rest`：`_rest_for_mood` 置位时选**住处(30, 回 mood NORMAL→GOOD→BEST)** 而非冥想室(60)；钱<30 → 露宿。`policy` 加 `_rest_for_mood` 标志。
- `live_loop` 传 `screenshot` + `policy` 给 `inspector.decide`。
- 新增测试：decide_early_training（选最多/全<=1休息/平局）+ rest_for_mood（住处优先/钱不够露宿）。全套 135 测试过。
- 详见 设计方案.md §12。

### OCR 批量化提速（17 倍，满足单轮 ≤30s）
- 实跑发现 classify_hybrid 一次 40s（逐区域 OCR~40 次×~1s），一轮训练 ~5 分钟，不可用。
- 诊断：全图 `read_lines`（ONNX Runtime GPU，1.9s 拿全部 text+bbox）+ 按 region rect 中心点匹配文字，比逐区域 `crop_region + read_text` 快 ~20 倍。
- `classifier_anchors._read_anchor_regions`：per-region OCR → 全图 `reader.read_names`（走 `RegionOcrReader` 缓存）。
- `RegionOcrReader`：所有 `read_*` 共享一次 `read_lines`（`self._lines` lazy 缓存），`read_where` 按 bbox 中心点匹配区域 → 一帧只 OCR 一次全图。
- classify + parse 共享同一个 `reader`：`classify_hybrid`/`classify_by_ocr` 接收 `reader`（`isinstance` 兼容旧 `ocr` 参数）；`live_loop` 创建 `reader`，分类+payload 读共用，parse 复用缓存≈0s。
- onnxruntime GPU 缺 cuDNN 9→CPU（慢 3 倍）→ 装 `nvidia-cudnn-cu13`（cuDNN 9.23）+ `nvidia-cublas-cu13`+`ocr.py` 模块级 PATH 注入→GPU 启用。
- 结果：classify 40s→2.88s、parse 1.91s→0、单帧 ≈2.4s、一轮训练（5-8 帧）≈12-20s **远低于 30s 目标**；137 测试全过。
- 详见 设计方案.md §13。

## 2026-07-04

### 裁剪头像不含进度条 + 新库命名 N.png
- **问题**：旧裁剪窗口 113px 高含底部 ~20px 羁绊进度条；训练后羁绊值变化导致进度条填充变化 → 模板 matchTemplate 分数下降到 0.85 阈值以下 → 漏识别 → 早期游戏训练决策错误。
- `collect_head_templates.py`：`_HEAD_H_FRAC` 从 `113/260` 改为 `93/260`（缩减高度排除进度条，裁剪尺寸 111×93）。
- 新库命名 `N.png`（纯数字，如 `1.png`/`2.png`，不含前缀）；新增 `_is_new_library_template(path)` 判断 `^\d+\.png$`。
- `_existing_template_ids`/`_next_template_id`/`_is_duplicate`/`_find_head_instances` 模板预读 全部改为扫描新库（纯数字 .png），不再比对旧库 `counting_head_*.png`/`non_counting_head_*.png`。
- `vision.count_heads`：glob 从 `counting_head_*.png` 改为 `*.png` + `_is_head_template` 过滤（`^\d+\.png$`）；`_HEAD_MATCH_THRESHOLD=0.85` 不变。
- 旧库 `counting_head_*.png`/`non_counting_head_*.png` 保留为遗留，不删除/不移动/不被 `count_heads` 引用。
- 详见 设计方案.md §14。

### _find_head_instances NMS 修复（loc_score 局部极大值）
- **Bug**：原 NMS 按"相邻窗口 y 差 < 60 合并"，但滑窗步长 5 < 60 → 整列所有窗口被合并成 1 组，只输出 1 个头像实例（实际画面有 3 个）。
- **扫描范围扩大**：`y_end` 从 `panel.y + panel.h - win_h` 改为 `image.h - win_h`（头像可能超出 panel 底部）。
- **双分数**：`loc_score`（所有模板含旧库最大分，用于定位）+ `dedup_score`（仅新库模板最大分，用于去重）。加载旧库模板做定位是因为新库模板少（初期仅 1.png），无法定位所有头像；旧库含进度条但 matchTemplate 仍能定位头像大致位置（分数 0.5-0.9）。
- **NMS 改用局部极大值**：在 `loc_score` 曲线上找局部峰值（score 比前后窗口都高且 > 0.3），相邻峰值 y 差 ≤ `_HEAD_NMS_DY=60` 时保留更高分的。修复后正确发现 3 个头像实例（y=155/260/370）。
- 详见 设计方案.md §14。

### 采集新头像入新库
- 运行 `collect_head_templates` 从当前训练选择画面采集 3 个支援卡头像入新库：`2.png`/`3.png`/`4.png`（`1.png` 已存在，新库共 4 个模板）。
- `count_heads` 验证：当前画面正确识别 3 个头像（与肉眼一致）。
- 137 测试全过（含 `test_count_heads_returns_zero_when_no_template_matches`）。

### 委托选择规则更新：+3 buffer 上限 + 全超退一阶
- **问题**：原 `decide_commission` 上限 = `character_rank`（rank 22 时上限 22），漏掉 rank 24 这种"超自己 2 级但可打"的高阶讨伐 → 实际选 rank 19（≤22 的最高），与用户预期"rank 22 最高能打 rank 25"不符。
- **新规则**：`COMMISSION_RANK_BUFFER = 3`（class 常量）。`limit = character_rank + 3`，doable = `[opt for opt in options if parse_first_int(opt.rank) ≤ limit]`，`best = max(doable)` 取最高。全部委托都超 limit 时退一阶：取所有委托里次高 rank 的（最高那档太硬打不动降一档，比无脑选最低阶更合理）。
- **红字优先级不变**：`has_red_text=True` 的委托仍优先于 rank 比较（红字 = 适合当前角色的委托，游戏 UI 提示），不进 buffer 规则。
- **实机场景**：rank 22 + 三委托 rank 17/19/24（无红字）→ 上限 25，doable={17,19,24}，max=24 → 选 rank 24 ✓（原逻辑选 19）。
- 新增测试：`test_picks_highest_doable_within_rank_plus_buffer`（rank 22 + 17/19/24 → 选 24，含两步确认）+ `test_falls_back_to_second_highest_when_all_exceed_limit`（rank 22 + 26/28/30 → 退一阶取 28）+ `tests/test_commission_full_flow.py` 端到端 5 帧（hub 红字 → 委托选 → accept → 战斗开战 → 战斗确认）+ 红字优先 + buffer 常量。142 测试全过（137→142）。
- 详见 设计方案.md §16。

### 奖励模板库（relics.json）+ 角色类型属性优先级 + 自动入库
- **问题**：旧 `decide_relic` 按数字 score 比大小选卡，但当前画面用效果文本（如「自身的最大生命力增加4%」）而非 score → score=None → pause 卡死。`relic_choice_card_N_description` 区域已定义但 parser 没读。
- **模板库**：新建 `config/relics.json`（schema=v1，初始 3 条：沾有史莱姆的铠甲/裤子/手套，OCR 真实采集入库）。结构类似 events.json，纯文本数据不存图像。
- **relic_db.py 新模块**：`load_relics`/`find_relic`/`save_relic`（upsert）/`extract_attributes`（关键词解析）/`class_to_priority_group`（角色类型→优先级组）/`score_relic_by_priority`（按组打分）。
- **角色类型 → 优先级组**：characters.json 6 类（刺客/术师/游侠/突击者=ATTACK；辅助/坦克=DEFENSE）。用户规则：游侠=弓手、突击者=战士，与刺客同组（输出系）。
  - ATTACK 组优先级：speed > crit_rate > crit_dmg > attack
  - DEFENSE 组优先级：speed > hp > defense
  - 第 1 位=100 分，第 2 位=90 分，依此类推（-10 递减）。
- **属性解析**：关键词表顺序敏感（"暴击率"先于"暴击伤害"避免前缀吞）。OCR 偶在中文词中插空格（如「生 命力」）→ 匹配前去空格兜底。
- **parse_relic_choice**：新增读 `relic_choice_card_N_description` → effect_text → `extract_attributes` → `RelicOption.attributes`。
- **decide_relic 重写**：加载模板库 → 查表填 attributes；未匹配 → `save_relic` 自动入库；按角色优先级组打分排序选最高；score 数字作同优先级二次排序（兼容旧画面）。
- **GameState 加 character_class**：`live_loop` 启动时按 `desired_character` 查 characters.json 填入；None → 默认 ATTACK 组。
- **TrainerPolicy 加 relic_db_path 参数**：默认 `config/relics.json`，测试可注入临时文件。
- **实机采集入库**：当前画面 3 张卡真实 OCR 入库（铠甲 hp/裤子 defense/手套 attack）。
- 新增 `tests/test_relic_db.py` 24 测试：extract_attributes（含 OCR 空格容错）+ 角色映射 + 优先级打分 + find/save + decide_relic 集成（ATTACK/DEFENSE 组选卡）+ 自动入库幂等。166 测试全过（142→166）。
- 详见 设计方案.md §17。

### OCR 引擎可配置（gpu/cpu/auto） + interval 默认 0.5s
- **OCR 引擎选择**：`RapidOcrEngine.__init__` 加 `engine` 参数（"auto"/"gpu"/"cpu"），`use_cuda` 兼容旧调用方（True→"gpu", False→"cpu"）。
- `cli/live_loop.py` 加 `--ocr-engine {auto,gpu,cpu}` 参数（默认 auto），传给 `_create_ocr(use_paddle, ocr_engine)`。
- `_is_cuda_active()` 检测 onnxruntime 是否真用了 CUDAExecutionProvider；engine=auto/gpu 但 CUDA EP 不可用时发 RuntimeWarning（提示用户 onnxruntime CPU 版本仍装着，需 `pip uninstall onnxruntime` 让 onnxruntime-gpu 接管）。
- 保留 onnxruntime 不卸载（用户要求）：两个包同时装时 engine=auto 自动检测，CUDA EP 不可用回退 CPU。
- `--interval` 默认 3.0s → 0.5s（live_loop 同步架构，interval 只是帧间等待，不影响 OCR；总延迟 = OCR + 0.5s + 决策 <1ms）。
- 详见 设计方案.md §18。

### 商品模板库（shop_items.json）+ 角色类型分类匹配 + 多件购买
- **问题**：旧 ShopInspector 按 `shop_buy_effect_keywords`（回体力/潜质点退还）关键词匹配买第一件，限购 1 件即退。用户需求：按角色类型选商品，可买多件，分类不匹配角色时停止。
- **模板库**：新建 `config/shop_items.json`（schema=v1）。每条：`{name, effect, price, classes, priority}`。classes 按角色类型分可多选（如 ["游侠","刺客","坦克"]），空=适合所有角色；priority 0-5（数字越小越优先，用户手填），99=自动入库默认（用户手填前不买）。
- **shop_db.py 新模块**：`load_shop`/`find_shop`/`save_shop`（upsert 保留 priority 不覆盖用户手填值）/`class_matches`（空分类=所有角色；多分类任一命中；无角色类型时空分类才匹配）/`should_buy`（class_matches + priority ≤ MAX_BUY_PRIORITY=5 + 未买过 三条都满足）/`filter_buyable`（按 priority 升序）。
- **ShopInspector 重写**：检视完所有行后查模板库（未入库的自动 save_shop priority=99）→ 按 `filter_buyable(character_class)` 排序 → 逐个买（两步确认 select→buy，可买多件不再限购 1 件）→ 无可买时退出（用户场景：界面只剩坦克/辅助，角色是游侠 → 退出）。
- **TrainerPolicy 加 shop_db_path 参数**：默认 `config/shop_items.json`，测试可注入临时文件。
- 新增 `tests/test_shop_db.py` 22 测试：class_matches/should_buy/filter_buyable/find/save/upsert 保留 priority + ShopInspector 集成（多件购买/角色不匹配退出/自动入库）。188 测试全过（166→188）。
- **Bug 修复**：`load_shop` 用 `or` 把 priority=0 当 falsy 替换成 99（修复为 `if priority is not None`）。
- 详见 设计方案.md §19。

### 不作数头像命名 + dedup 扫描集扩展 + NMS std 阈值提高
- **问题**：用户标识 `2.png`/`3.png` 为不作数头像（前期训练决策不应计数）；旧 `dedup_score` 仅扫 `N.png`，导致同一颗不作数头反复入库污染新库；`_HEAD_EMPTY_STD=25` 过低，远 panel 噪声区（std≈25）会被 NMS 当峰值误识别（实测 y=1205 处 std=25.8、loc_score=0.359 被误判为头像）。
- **不作数命名**：沿用旧库 `non_counting_head_N.png` 风格（旧库已有 1/2，新库从 3 开始）。`2.png` → `non_counting_head_3.png`、`3.png` → `non_counting_head_4.png`。`count_heads._is_head_template` 仅匹配 `^\d+\.png$`，自动跳过 non_counting ✓ 不需改 `vision.py`。
- **dedup 扫描集扩展**：新增 `_is_non_counting_template`/`_is_dedup_template`，`dedup_templates` 加载 `N.png`（作数）+ `non_counting_head_N.png`（不作数）——避免同一颗头无论作数/不作数反复入库。旧库 `non_counting_head_1/2.png`（旧几何 113×113）会被加载，但因尺寸 > 新窗口 111×93 自动跳过；旧库 `counting_head_*.png` 不参与（含进度条，几何不同）。
- **NMS std 阈值**：`_HEAD_EMPTY_STD` 从 25 提到 40。实测：真头像 std 73-98（远高于 40），误识别区 std≈25（远低于 40），干净分离。
- **重新采集 4.png**：旧 4.png 与当前画面 slot4（y=585）匹配分仅 0.822（顶部 std 差异大：4.png 顶 95.5 / slot4 顶 59.3，画面状态变化）→ 用 slot4 内容覆盖 4.png，dedup_score 提升至 0.96。
- **入库新头像**：当前力量训练画面采集 3 个新头像入新库：`5.png`/`6.png`/`7.png`（槽 1/2/3，y=260/370/475）。
- `count_heads` 验证：当前画面返回 4（作数头 4/5/6/7.png，slot0=non_counting_head_3 不计数）。
- 137 测试全过。
- 详见 设计方案.md §15。

### ShopInspector 默认打开首个跳过点击 #1 + 详情面板刷新等待 + NEVER_BUY_PRIORITY
- **问题 1（默认打开首个）**：进入交易界面后游戏默认打开第一个商品的详细面板，此时再点击第一个商品会**关闭**详细面板（与点击其他行相反，其他行是打开）。旧 inspector 仍按 idx=1 点击 → 关闭面板 → 下一帧 selected_effect 空 → 误判 #1 未读到 → 卡死或重复点击。
- **修复**：ShopInspector 新增 step 1.5（[shop_inspector.py:67-75](file:///d:/starsavior-trainer-v2/starsavior_trainer/inspectors/shop_inspector.py#L67-L75)）：首次进入交易界面（`not self.effects and self.pending_index is None and scene.selected_effect.strip()`）时直接把当前 selected_effect 当作 #1 的 effect 记录，跳过点击 idx=1，从 idx=2 开始检视。
- **问题 2（详情面板未刷新）**：live_loop interval=0.5s 太快，点击 #X 后下一帧 OCR 时游戏详情面板还没刷新 → OCR 仍读到上一个商品的 effect → 全部商品 effect 都被录入成 #1 的（用户反馈"全部录入的商品1的详细"）。
- **修复**：新增 `last_seen_effect` + `settle_wait` 字段（[shop_inspector.py:41-44](file:///d:/starsavior-trainer-v2/starsavior_trainer/inspectors/shop_inspector.py#L41-L44)）。step 1 检查 `new_effect == last_seen_effect` 时 return `Action("skip", ...)` 等待刷新，最多等 3 帧（`settle_wait < 3`），3 帧后强制记录避免死循环（真相同 effect 的商品也会被记录）。
- **问题 3（priority 99 语义模糊）**：原 `DEFAULT_AUTO_PRIORITY = 99` 但 99 的语义不明确（是"永不买"还是"未手填"？）。
- **修复**：新增 `NEVER_BUY_PRIORITY = 99` 常量，`DEFAULT_AUTO_PRIORITY = NEVER_BUY_PRIORITY`（[shop_db.py:42-47](file:///d:/starsavior-trainer-v2/starsavior_trainer/shop_db.py#L42-L47)）。文档明确三档：0-5=会买（0 最优先）、6-98=低优先级不买、99=永不买（绝对不买；自动入库默认也是 99，用户手填前不买）。
- **shop handler ocr_prefixes 扩展**：`screens/__init__.py` shop handler 的 ocr_prefixes 从 `["shop_detail"]` 改为 `["shop_item", "shop_detail", "shop_buy", "shop_back"]`（让 inspector 能读到商品列表 name/price + buy/back 按钮坐标）。
- 新增测试：`test_wait_for_detail_refresh_before_recording`（点击 #2 后 selected_effect 未变化时等待）+ `test_force_record_after_3_waits_when_effects_genuinely_same`（等 3 帧后强制记录）+ `_drive_inspection` 辅助方法循环调用兼容等待逻辑。196 测试全过（188→196）。
- 详见 设计方案.md §20。

### D-DAY 评鉴战循环修复（_dday_rating_done 标志）
- **Bug**：D-DAY 评鉴战日大厅布局变成「评鉴战」+「交易」两个按钮。原逻辑先交易（`_dday_trading_done`）再点评鉴战按钮。但评鉴战打完后返回大厅，`rating_battle_button` OCR 仍命中"评鉴战"文字 → 又点评鉴战 → 死循环。
- **修复**：TrainerPolicy 新增 `_dday_rating_done: bool` 标志（[policy/engine.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/engine.py)）。`_decide_training_hub` 检查：`rating_battle_button is not None` 时若 `_dday_rating_done=True` 不再点，fall through 走正常 hub 流程（点 training_button）；非 D-DAY 时清两个标志（`_dday_trading_done=False` + `_dday_rating_done=False`）。
- **流程**：D-DAY 日 → 先交易（`_dday_trading_done=True`）→ 点评鉴战（`_dday_rating_done=True`）→ 评鉴战完成后返回大厅 → OCR 仍命中评鉴战文字但 `_dday_rating_done=True` → fall through 点 training_button → 正常训练。
- 新增 `tests/test_dday_hub.py` 5 测试：首次进 D-DAY 先点交易 / 交易完成点评鉴战 + 设标志 / 评鉴战完成后 fall through 不死循环 / 非 D-DAY 清标志 / 完整流程 + 下次 D-DAY 重新走流程。
- 196 测试全过。
- 详见 设计方案.md §21。

### T18 subtask 1 — classifier 消歧测试迁移（5 视觉测试延后）
- **背景**：旧 `d:/chengfeng/tests/test_classifier.py`（31 测试）是单屏状态机架构核心的回归保护——BATTLE vs TRAINING_HUB、SHOP vs BATTLE、reward vs CHARACTER_SELECT、rest_submenu/shop vs battle overlap 等历史坑台账 A 类（#1-#11）的误判防护网，新项目此前 0 覆盖。
- **迁入 26 测试** → `tests/test_classifier.py`：24 个 `_match_screen` payload 消歧 + 2 个 `_has_real_event_options`（空图 + `_FixedOcr` stub）。纯 1:1 搬迁，仅调整 import 路径（classifier 已拆 3 文件：`_match_screen`←classifier_anchors、`_has_game_menu_signature`/`_has_training_select_signature`←classifier_signatures、`_has_real_event_options`←classifier）、`\uXXXX`→中文字符。
- **延后 5 个视觉测试**：`test_visual_journey_origin_detects_*`（3 个）/ `test_hybrid_visual_override_*`（2 个）依赖 4 张真实截图（character_select_003 / blessing_setup_empty_001 / blessing_setup_ready_001 / blessing_choice_002），新旧项目均未入库 → 归 T18 subtask 2（真实 OCR/截图评估）真机范畴。
- 196 → 222 测试全过。
- 无设计方案配套（纯测试迁移 + 数据缺失延后决策，非架构/逻辑变更）。

### 缺口2 — blue 模式（无 OCR）训练死锁修复（decide_training 加 blue 降级分支）
- **Bug**：blue_parsers 读不到失败率 → 全卡 `fail_rate=None` → `engine.training_score` 一律 `-inf`（None 在 OCR 模式表示"未检视卡不可赌"，语义正确）→ decide_training 走"全失败率过高回 hub 休息"分支 → 永远休息无法训练（死锁）。
- **修复**：[policy/training.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/training.py) decide_training 入口加 blue 检测——`all(c.fail_rate is None for c in choices)`（blue 特征）→ 走新增 `_blue_training_score`（ring 颜色 + build 属性偏好，绕过 fail_rate）选一张盲训，reason "blue mode ... (no OCR, blind)"。**不动 engine.training_score**（保 OCR 模式 None=-inf 的安全语义，OCR 路径行为不变）。
- **命名红线落地**：docstring + 注释钉死"训练资源耐力=endurance，体力=stamina(HP 属性)"；原注释 "low stamina"（实指耐力低）改为 "low endurance — 训练资源耐力, NOT 体力/HP stamina"。
- 新增 `tests/test_policy_decisions.py` 4 测试：blue 选最佳 ring / build bias 打破 ring 平局 / 两步 confirm / 部分 None 不算 blue（回归）。222 → 226 测试全过。
- 详见 设计方案 §22.2。

### 缺口1 — 耐力（endurance）条 CV 读取链路（真机验证 endurance 84/98）
- **背景**：耐力（训练资源）此前仅靠失败率间接感知（全失败率高才被动休息）。现直接 CV 读顶部 HUD 绿色进度条 → `endurance_ratio`，为缺口3主动休息策略铺路。
- **CV 算法**（[vision.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/vision.py) `estimate_endurance_ratio`）：耐力条为绿色渐变条（蓝绿→黄绿）+ 灰底；用饱和度（`g>r+30 或 g>b+30`）区分填充绿 vs 暗绿底色（避免把未填充底色误判为绿）；从左连续前缀（容忍 ≤2 列间隙）/ 容器宽 = 填充比例。
- **改动**：
  - [regions/2560x1440.json](file:///d:/starsavior-trainer-v2/config/regions/2560x1440.json)：`training_hub_stamina_bar`/`training_select_stamina_bar` → `endurance_bar`；坐标 `[800,55,480,35]` → `[945,60,350,40]`（真机像素剖面标定——原坐标偏左偏窄，框到复杂背景）。代码零引用，改名零风险。
  - `vision.py`：新增 `estimate_endurance_ratio(image, rect)`（numpy 饱和度阈值 + 连续前缀）。
  - `models.py`：`TrainingHubStatus` 加 `endurance_ratio: float = 0.0`。
  - [screens/training.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/screens/training.py)：`parse_training_hub` 读 `endurance_bar` → `endurance_ratio` + `logger.info` 输出。
- **真机验证**：hub_check → endurance~84，training_select_v2 → ~98（绝对值待更多耐力档位图校准；相对值正确，两张图耐力不同证实时变条）。
- 新增 `tests/test_vision.py` 3 测试（partial 0.6 / empty 0.0 / full >0.95）。226 → 229 全过。
- **命名红线落地**：region/字段/CV 函数全用 `endurance`，绝不用 `stamina` 指训练资源。
- **范围说明**：本次仅"读取链路"（§22.1.1-3）；"endurance<40 主动休息"决策（§22.1.4）并入缺口3休息策略重构（综合 mood+endurance+coins，依赖 mood 读取）。
- 详见 设计方案 §22.1。

### 缺口3 — 休息策略重构（§22.3 决策树）+ mood 读取 + §22.4 失败率动态阈值
- **mood 读取**：[regions/2560x1440.json](file:///d:/starsavior-trainer-v2/config/regions/2560x1440.json) 加 `training_hub_mood_label`/`training_select_mood_label` `[1355,60,145,45]`（心情文字 BEST/NORMAL/GOOD 实际 x1357-1485）；`TrainingHubStatus` 加 `mood: str | None`；[screens/training.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/screens/training.py) `_parse_mood`（OCR 容错）+ parse_training_hub 读取。真机验证 live_frame→BEST、hub_check→NORMAL。
- **§22.4 失败率动态阈值**：[config.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/config.py) 加 `fail_rate_gain_high=50` + `fail_rate_threshold_low/mid/max=20/35/80`；[engine.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/engine.py) `training_score` 用 `_max_fail_for_gain(gain)` 替换固定 `max_training_fail_rate=30`（inspector 仍用旧阈值；用户规则：gain<50→20%、50-99→35%、≥100→80%）。
- **decide_rest §22.3 决策树**：[engine.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/engine.py) policy 加 `_cached_mood`/`_cached_endurance`（`_decide_training_hub` 从 TRAINING_HUB payload 更新）；[simple.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/policy/simple.py) decide_rest 重构——心情≠BEST（或 _rest_for_mood）→住处；心情BEST+耐力<40%→有钱冥想/没钱露宿；心情BEST+耐力≥40%→露宿。config 加 `rest_endurance_threshold=0.40`。
- **coin_count region 修正（现成 bug）**：真机验证暴露 `training_hub/select_coin_count` `[1420,58,120,55]` 偏左（落在 mood 文字区，coins 读 None）；金币数字实际在 x1552-1599，改 `[1530,58,110,45]`，coins 现读 245 ✓。
- **真机端到端**：live_frame（endurance 33% + BEST + 245）→ decide_rest = meditation_room ✓（符合 §22.3：耐力低+有钱）。
- 单测：§22.4 动态阈值 1 测试 + decide_rest §22.3 决策树 4 测试。229 → 234 全过。
- 详见 设计方案 §22.3/§22.4。

### §22.4 失败率模型改线性（替代分段，用户二次反馈）
- **反馈**：分段（gain<50→20%、50-99→35%、≥100→80%）不够精细；用户要 50→35%、60→40% 连续放宽，且 200 gain 时 99% 失败率也可赌（高 gain 高方差赌一把）。
- **改线性**：`max_fail = min(99, fail_rate_base + fail_rate_slope×gain)`，默认 `fail_rate_base=10` + `fail_rate_slope=0.5`。删 config 分段字段（`fail_rate_gain_high`/`fail_rate_threshold_low/mid/max`），加 `fail_rate_base`/`fail_rate_slope`。
- **理由**：训练成功=+gain，失败=0+降心情(可恢复)；gain 越高收益越大于失败代价，线性放宽失败率上限。实测：gain=20→20%(普通保守)、50→35%、60→40%、100→60%、200→99%。
- §22.4 单测改线性边界。234 测试全过。
- 详见 设计方案 §22.4。

### §22.5 支援卡羁绊达标 → 人头不作数（用户需求）
- **需求**：早期游戏"优先人头最多训练"（decide_early_game）加规则——**羁绊进度条变黄（达标）的人头从作数变不作数**，优先未达标支援卡所在训练（让全员尽快达标触发闪光训练，闪光训练由游戏机制自动触发，不需 bot 实现）。
- **实现**：[vision.py](file:///d:/starsavior-trainer-v2/starsavior_trainer/vision.py) 加 `_is_bond_maxed`（matchTemplate 拿人头 max_loc → 检测下方 +3~+20px 进度条黄色 HSV 占比 > 20%）；`count_heads` 扩展（达标人头不计入作数，只 decide_early_game 调用，不影响其他）。
- **真机验证**：`screenshots/支援卡羁绊达标.png` 保护训练人头（7.png match=0.96）→ 达标=True，count_heads=0（排除达标）。
- 新增 `tests/test_vision.py` 2 测试（黄/非黄进度条 → 达标 True/False）。234 → 236 全过。
- 详见 设计方案 §22.5。
